import os
# select the directory whose content you want to list
path = "/"
# use the os module to list the directory content
contents = os.listdir(path)
# print the cotent of the directory
print(contents)
